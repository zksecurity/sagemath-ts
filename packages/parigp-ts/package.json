{
  "name": "@sagemath-ts/parigp-ts",
  "version": "0.0.14",
  "description": "TypeScript port of PARI/GP number theory functions",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "license": "GPL-2.0-or-later"
}
